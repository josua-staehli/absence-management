// The generated client, plus the two hand-written pieces around it: the ApiError every caller
// catches, and the interceptor that creates it. Importing `./lib/client` is what registers it.
export * from './generated';
export * from './lib/api-error';
export * from './lib/client';
