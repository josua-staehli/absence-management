# @absence-management/employees-data-access

The employee list as a TanStack Query hook over the generated client. `scope:employees`,
`type:data-access` — it is what the absence request form fills its employee select from, and what
the employees page reads.
